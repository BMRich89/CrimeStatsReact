# Environment variables loaded into the Codespaces container.
# Edit API_HOST to point to your API's Codespaces forwarded URL
# (e.g. https://<codespace-name>-7015.app.github.dev) when running
# the API in a separate Codespace, then rebuild the container.
API_HOST=http://localhost:7015
